#pragma once

#include <cstdint>
#include <string>

namespace epseon {
    namespace gpu {
        namespace common {
            std::string vulkan_version_to_string(uint32_t);
        }
    } // namespace gpu
} // namespace epseon
